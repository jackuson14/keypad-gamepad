# --- Digital mapper path (mapper.py / gui.py) ---
vgamepad>=0.1.0
keyboard>=0.13.5
mouse>=0.7.1

# --- Analog path (the analog gamepad engine) ---
# `hid` is the ctypes binding to the hidapi shared library. It is what reads
# the keyboard's real-time analog key-depth events.
hid>=1.0.6

# --- Analog GUI look (Windows 11-style dark/light theme for ttk widgets) ---
sv-ttk>=2.6.0

# --- Analog GUI tray icon (optional; analog_gui degrades gracefully without) ---
pystray>=0.19.0
Pillow>=10.0.0

# --- In-app update check (TLS CA bundle). Required so the frozen .exe can verify
# HTTPS to GitHub on a clean machine; CPython's ssl ignores the Windows cert store. ---
certifi>=2024.0.0
